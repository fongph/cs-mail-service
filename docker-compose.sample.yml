version: '2'
services:
    application:
        build: .
        volumes:
            - .:/code
        environment:
            - QUEUE_HOST=
            - QUEUE_PORT=
            - QUEUE_USER=
            - QUEUE_PASSWORD=
            - QUEUE_PROVIDER_KEY=
            - QUEUE_FAILED_KEY=
            - SMTP_HOST=
            - SMTP_PORT=
            - SMTP_SECURITY=
            - SMTP_USER=
            - SMTP_PASSWORD=